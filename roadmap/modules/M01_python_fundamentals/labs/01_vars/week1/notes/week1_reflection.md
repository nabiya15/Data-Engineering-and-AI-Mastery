I learned how input() always returns a string and how f-strings inject variables directly into text.
The biggest win was seeing the terminal story change based on user answers — felt like real interactivity.
